"use client"; import {useEffect,useState} from 'react'; import {formatRupiah} from '@/lib/utils'; import {ResponsiveContainer,LineChart,Line,XAxis,YAxis,Tooltip,CartesianGrid} from 'recharts';
export default function Page(){const [data,setData]=useState<any>(null);useEffect(()=>{fetch('/api/analytics').then(r=>r.json()).then(setData)},[]);if(!data)return <div className="p-6"><p className="text-sm text-slate-500">Memuat analisis...</p></div>;return <div className="p-6 lg:p-8"><div className="mx-auto max-w-6xl"><h1 className="text-2xl font-semibold">Analisis</h1><p className="mt-1 text-sm text-slate-500">Tren pembayaran, pengeluaran, dan kewajiban berdasarkan transaksi nyata.</p><div className="mt-6 border border-line bg-white p-5 shadow-soft"><h2 className="font-semibold">Pembayaran dan pengeluaran</h2><div className="mt-5 h-80"><ResponsiveContainer width="100%" height="100%"><LineChart data={data.monthly}><CartesianGrid strokeDasharray="3 3" vertical={false}/><XAxis dataKey="month" tick={{fontSize:11}}/><YAxis tick={{fontSize:11}}/><Tooltip formatter={(v)=>formatRupiah(Number(v))}/><Line type="monotone" dataKey="totalPaid" name="Pembayaran" strokeWidth={2}/><Line type="monotone" dataKey="totalExpense" name="Pengeluaran" strokeWidth={2}/></LineChart></ResponsiveContainer></div></div></div></div>}
