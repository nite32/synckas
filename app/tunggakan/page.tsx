"use client"; import {useEffect,useState} from 'react'; import {MONTHS} from '@/lib/constants'; import {formatRupiah} from '@/lib/utils'; import {StatusBadge} from '@/components/ui/StatusBadge';
export default function Page(){const [month,setMonth]=useState(MONTHS[0]),[rows,setRows]=useState<any[]>([]);useEffect(()=>{fetch('/api/outstanding?month='+encodeURIComponent(month)).then(r=>r.json()).then(setRows)},[month]);return <div className="p-6 lg:p-8"><div className="mx-auto max-w-6xl"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><h1 className="text-2xl font-semibold">Tunggakan</h1><p className="mt-1 text-sm text-slate-500">Status kewajiban setiap anggota pada bulan yang dipilih.</p></div><select value={month} onChange={e=>setMonth(e.target.value)} className="border border-line bg-white px-3 py-2 text-sm">{MONTHS.map(m=><option key={m}>{m}</option>)}</select></div><div className="mt-6 overflow-x-auto border border-line bg-white shadow-soft"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="px-5 py-3">Nama</th><th className="px-5 py-3 text-right">Wajib</th><th className="px-5 py-3 text-right">Dibayar</th><th className="px-5 py-3 text-right">Kekurangan</th><th className="px-5 py-3">Status</th></tr></thead><tbody className="divide-y divide-line">{rows.map(r=><tr key={r.member.id}><td className="px-5 py-3 font-medium">{r.member.name}</td><td className="px-5 py-3 text-right">{formatRupiah(r.required)}</td><td className="px-5 py-3 text-right">{formatRupiah(r.paid)}</td><td className="px-5 py-3 text-right">{formatRupiah(r.shortfall)}</td><td className="px-5 py-3"><StatusBadge status={r.status}/></td></tr>)}</tbody></table></div></div></div>}
